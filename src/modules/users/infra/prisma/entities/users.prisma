model User {
  id          String    @id @default(uuid())
  name        String
  email       String    @unique
  password    String
  phone       String
  cpf         String    @unique
  nascimento  DateTime
  
  created_at  DateTime  @default(now())
  updated_at  DateTime  @updatedAt


  @@map("user")
}
